services:
  backend:
    build: ./Backend
    container_name: jobportal-api
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: jobportal-frontend
    ports:
      - "5173:5173"